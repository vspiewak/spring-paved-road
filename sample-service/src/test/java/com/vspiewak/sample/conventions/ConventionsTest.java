package com.vspiewak.sample.conventions;

import com.vspiewak.pavedroad.conventions.PlatformConventionsTest;

/** The platform conventions, opted into — the whole point is that this class is empty. */
class ConventionsTest extends PlatformConventionsTest {}
